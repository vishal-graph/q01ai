{
  "version": 3,
  "buildCommand": "pnpm install --frozen-lockfile && pnpm run build",
  "outputDirectory": "public",
  "routes": [
    { "src": "/(healthz|readyz|questionnaires(?:/.*)?|admin(?:/.*)?)", "dest": "/api/questionnaire" }
  ]
}
